# Digit-Detection
Please make sure to first run the commented program at the top if you decide not to download the "digit_model.h5" file. Afterwards, just run the main program. Some modules may need to be installed via the Python console using the "pip install" command.
