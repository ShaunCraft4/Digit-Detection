# Digit-Detection
If you decide not to download the "digit_model.h5" file, please make sure first to run the commented program at the top. Afterwards, just run the main program. Some modules may need to be installed via the Python console using the "pip install" command.
